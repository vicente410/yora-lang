# Showcase of Yora 0.1.0, written by Bruno Vicente.
#
# This program calculates an exponentiation. To do so,
# it multiplies the base by itself exponent times.
#
# Exit is limited to 8 bits so results larger than 255
# will not be correctly displayed.

var base = 2
var exponent = 7

var result = 1
var i = 0 # iteration

while i < exponent # repeat exponent times
	result *= base 
	i += 1 # next iteration

exit(result) # exit with the result
